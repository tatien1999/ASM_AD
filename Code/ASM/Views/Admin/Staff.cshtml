@model IEnumerable<ASM.Models.User_Account>

@{
    ViewBag.Title = "Staff";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}




<div id="content-header">
    <div id="breadcrumb"> <a href="#" title="Go to Home" class="tip-bottom"><i class="icon-home"></i> Home</a> <a href="#" class="current">Account</a> </div>
    <h1>Account</h1>
</div>
<div class="container-fluid">
    <hr style="margin : 10px 0;">
    <div class="row-fluid">
        <div class="span12">
            <div class="widget-box" style="margin-top: 0px;">
                <div class="widget-title">
                    <span class="icon"><i class="icon-th"></i></span>
                    <button type="button" class="btn btn-default">@Html.ActionLink("Add Account", "AddAccount", "Admin")</button>
                    <div class="input-group" style="float: right">
                        <input type="text" id="Search" placeholder="Search" class="form-control" />
                    </div>
                    <h5>Data table</h5>
                </div>
                <div class="widget-content nopadding">
                    <table class="table table-bordered ">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Username</th>
                                <th>Password</th>
                                <th>Position</th>
                                <th colspan="2">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="Search">
                                    <td>
                                        @item.UserID
                                    </td>
                                    <td>
                                        @item.UserName
                                    </td>
                                    <td>
                                        @item.Password
                                    </td>
                                    <td>
                                        @item.Position
                                    </td>
                                    <td class="center"><a href="@Url.Action("EditAccount", "Admin" , new {id = item.UserID })">Edit</a></td>
                                    <td class="center"><a href="@Url.Action("DeleteAccount", "Admin" , new {id = item.UserID })">Delete</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script>
    $(document).ready(function () {
        function Contains(text_one, text_two) {
            if (text_one.indexOf(text_two) != -1)
                return true;
        }
        $("#Search").keyup(function () {
            var searchText = $("#Search").val().toLowerCase();
            $(".Search").each(function () {
                if (!Contains($(this).text().toLowerCase(), searchText)) {
                    $(this).hide();
                }
                else {
                    $(this).show();
                }
            });
        });
    });
</script>
